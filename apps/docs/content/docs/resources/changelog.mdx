---
title: Changelog
description: MarzbanSDK, marzban-mcp, and marzban-cli release history — generated directly from each package's CHANGELOG.md, so it can't drift from what's on npm or GitHub Releases.
full: true
---

Each release below is parsed straight from `packages/*/CHANGELOG.md` — the same [git-cliff](https://git-cliff.org) output that ships inside the npm package and becomes the GitHub Release notes. There's no hand-maintained copy to fall out of sync.

<ChangelogViewer />

<Callout type="info">
  Upgrading across a breaking release? See the [Migration guide](/docs/resources/migration).
</Callout>
