{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**", "out/**", "!.next/cache/**"]
    },
    "marzban-sdk-docs#build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "out/**", "!.next/cache/**"],
      "inputs": ["$TURBO_DEFAULT$", "$TURBO_ROOT$/packages/*/CHANGELOG.md"]
    },
    "lint": {},
    "test": {
      "dependsOn": ["^build"]
    },
    "types:check": {
      "dependsOn": ["^build"]
    },
    "codegen": {
      "cache": false
    }
  }
}
